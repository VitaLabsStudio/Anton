#!/bin/sh
if git diff --cached --name-only | grep -E " [0-9]+\.(ts|js|json|md)$"; then
  echo "Error: Duplicate file detected (e.g., file 2.ts). Please remove numbered duplicate files before committing."
  exit 1
fi
