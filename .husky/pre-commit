#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🛡️ Running pre-commit checks..."
echo "💡 TIP: To bypass, run 'git commit --no-verify'"

pnpm lint-staged || {
  echo "❌ Lint-staged checks failed. Please fix the issues above."
  echo "💡 To bypass this check in an emergency, run: git commit --no-verify"
  exit 1
}

echo "✅ Pre-commit checks passed."
